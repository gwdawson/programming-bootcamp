// Predict -> Observe -> Explain

function double(num) {
  num * 2;
}

const result1 = double(10);
console.log(result1);

// Prediction => prints 20 to the terminal
// Actual => prints undefined (there is no return)
