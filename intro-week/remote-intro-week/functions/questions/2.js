// Predict -> Observe -> Explain

function sum(a, b, c) {
  const total = a + b + c;
  return total;
}

const output1 = sum(100, 20, 4);

// Prediction => nothing, there is no console.log()
// Actual => nothing, the is no console.log()
